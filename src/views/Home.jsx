const React = require('react');
const Layout = require('./Layout');

function Home(props) {
  return (
    <Layout title="WebSocket">
      <h1>Home page</h1>
    </Layout>
  );
}

module.exports = Home;
